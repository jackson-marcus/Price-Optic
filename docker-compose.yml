services:
  api:
    build: .
    env_file: .env
    ports:
      - "8120:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      PRICEOPTIC_API_URL: http://api:8000
    ports:
      - "8621:8501"
    depends_on:
      - api
